name: 🚀 Deploy to Production (Password Auth)

on:
    push:
        branches: [ main ]
    workflow_dispatch:

env:
    DEPLOY_PATH: /var/www/symfony

jobs:
    deploy:
        name: 🚀 Deploy to VPS
        runs-on: ubuntu-latest

        steps:
            - name: 📥 Checkout code
              uses: actions/checkout@v4

            - name: 🔧 Install sshpass
              run: sudo apt-get update && sudo apt-get install -y sshpass

            - name: 🚀 Deploy to VPS with password
              env:
                  VPS_PASSWORD: ${{ secrets.VPS_PASSWORD }}
                  VPS_USER: ${{ secrets.VPS_USER }}
                  VPS_HOST: ${{ secrets.VPS_HOST }}
              run: |
                  # Créer un script temporaire pour éviter d'exposer le mot de passe
                  cat > deploy_script.sh << EOF
                  set -e

                  echo "🔄 Starting deployment..."

                  #Creer le repertoire
                  mkdir /var/www/symfony

                  # Aller dans le répertoire du projet
                  cd /var/www/symfony

                  # Afficher l'état actuel
                  echo "📍 Current directory: $(pwd)"
                  echo "📊 Git status before update:"
                  git status --porcelain || true

                  # Sauvegarder les changements locaux si nécessaire
                  if [ -n "$(git status --porcelain)" ]; then
                    echo "💾 Stashing local changes..."
                    git stash push -m "Auto-stash before deployment $(date)"
                  fi

                  # Mettre à jour le code
                  echo "📥 Pulling latest changes..."
                  git fetch origin
                  git reset --hard origin/main

                  # Afficher le commit déployé
                  echo "✅ Deployed commit: $(git log -1 --format='%h - %s (%an, %ar)')"

                  # Exécuter make prod
                  echo "🏗️ Running make prod..."
                  make prod

                  echo "🎉 Deployment completed successfully!"
                  EOF

                  # Exécuter le script sur le serveur via SSH avec mot de passe
                  sshpass -p "$VPS_PASSWORD" ssh -o StrictHostKeyChecking=no "$VPS_USER@$VPS_HOST" 'bash -s' < deploy_script.sh

                  # Nettoyer le script temporaire
                  rm deploy_script.sh

            - name: 🧪 Test deployment
              env:
                  VPS_HOST: ${{ secrets.VPS_HOST }}
              run: |
                  echo "🔍 Testing deployment..."
                  sleep 10

                  # Test de connectivité
                  if curl -f -s --max-time 10 "http://$VPS_HOST:8000" > /dev/null; then
                    echo "✅ Application is responding"
                  else
                    echo "⚠️ Application might not be responding correctly"
                    curl -s -o /dev/null -w "HTTP Status: %{http_code}\n" "http://$VPS_HOST:8000" || echo "Failed to connect"
                  fi

            - name: 📢 Deployment result
              if: always()
              run: |
                  if [ "${{ job.status }}" = "success" ]; then
                    echo "✅ Deployment successful!"
                    echo "🌐 Application: http://${{ secrets.VPS_HOST }}:8000"
                  else
                    echo "❌ Deployment failed!"
                  fi
